package io.github.connellite.microorm.query;

/** SQL sort direction used by an {@link Order}. */
public enum OrderDirection {
    /** Ascending order. */
    ASC,
    /** Descending order. */
    DESC
}
