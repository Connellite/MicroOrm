/**
 * Entity metadata built from registered classes: table/column mapping, relation fields, naming strategies,
 * and graph persistence helpers used internally by {@link io.github.connellite.microorm.session.Session}.
 */
package io.github.connellite.microorm.mapping;
