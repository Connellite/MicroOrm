/**
 * {@link io.github.connellite.microorm.session.Session} — unit of work over a JDBC connection for
 * entity CRUD, schema helpers, streaming reads, and association loading.
 */
package io.github.connellite.microorm.session;
